import { TlCurrencyPipe } from './tl-currency.pipe';

describe('TlCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new TlCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
